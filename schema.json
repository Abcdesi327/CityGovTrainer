{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/CityGovTrainer/quiz-data/schema.json",
  "title": "CityGovTrainer Question",
  "type": "object",
  "required": ["id", "type", "competencies", "difficulty", "prompt", "explanation"],
  "additionalProperties": false,
  "properties": {
    "id": {
      "type": "string",
      "pattern": "^[a-z0-9]+(-[a-z0-9]+)*$",
      "description": "Stable slug. Never reuse or renumber."
    },
    "type": {
      "enum": ["multiple-choice", "multi-select", "ordering", "numeric", "open-response"],
      "description": "open-response has no machine-gradable answer; it shows model reasoning after the user commits an answer."
    },
    "case_study": {
      "type": "string",
      "description": "Filename in /case-studies this question draws on. Omit for standalone questions."
    },
    "competencies": {
      "type": "array",
      "minItems": 1,
      "items": { "type": "string" },
      "description": "IDs from taxonomy/competencies.json. Subtopics allowed."
    },
    "difficulty": { "type": "integer", "minimum": 1, "maximum": 3 },
    "scenario": {
      "type": "string",
      "description": "Situational setup shown before the prompt. Keep under ~200 words."
    },
    "prompt": { "type": "string" },
    "options": {
      "type": "array",
      "items": {
        "type": "object",
        "required": ["id", "text"],
        "properties": {
          "id": { "type": "string" },
          "text": { "type": "string" },
          "rationale": {
            "type": "string",
            "description": "Why this option is right or wrong. Shown per-option after answering."
          }
        }
      }
    },
    "answer": {
      "description": "String for multiple-choice, array for multi-select/ordering, number for numeric, null for open-response.",
      "type": ["string", "array", "number", "null"]
    },
    "answer_tolerance": {
      "type": "number",
      "description": "For numeric questions: acceptable absolute deviation."
    },
    "explanation": {
      "type": "string",
      "description": "The teaching content. Required on every question — this is the point of the app."
    },
    "data_reference": {
      "type": "object",
      "description": "Optional link to a dataset backing a projection-style question.",
      "properties": {
        "source": { "type": "string" },
        "url": { "type": "string" },
        "retrieved": { "type": "string", "format": "date" },
        "notes": { "type": "string" }
      }
    },
    "further_reading": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "title": { "type": "string" },
          "url": { "type": "string" }
        }
      }
    },
    "tags": { "type": "array", "items": { "type": "string" } }
  }
}
